#include "main.h"

/**
 * reverse_array - function return order
 *
 * @a: input
 * @n: input
 *
 * Return: 0
*/
void reverse_array(int *a, int n)
{
	int i, j, t;

	for (i = 0; i < n - 1; i++)
		b[i] = a[n - i - 1];

	return (b);
}
