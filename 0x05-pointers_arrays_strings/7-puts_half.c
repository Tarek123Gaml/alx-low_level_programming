#include "main.h"

/**
 * puts_half - print half string
 *
 * @str: takes string as input
 *
 * Return: 0
*/

void puts_half(char *str)
{
	int l = 0;
	int n = 0;

	while (str[l] != '\0')
		l++;

	i++;
	for (i /= 2; str[i]; i++)
		_putchar(str[i]);

	_putchar('\n');
}
